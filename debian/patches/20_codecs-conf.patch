Author: Cristian Greco <cristian.debian@gmail.com>
Description: Set default encoders and decoders for Debian to packages
availables in main section. In particular, set 'twolame' as default mp3 encoder
and 'ffmpeg' as default tta decoder.
Index: pacpl-4.0.5/codecs.conf
===================================================================
--- pacpl-4.0.5.orig/codecs.conf	2011-01-05 19:04:56.000000000 +0100
+++ pacpl-4.0.5/codecs.conf	2011-01-05 19:04:58.000000000 +0100
@@ -1,6 +1,17 @@
 # specify the default encoder/decoder for each file format.
 # 
 # FORMAT = encoder,decoder <-- must look exactly like this.
+#
+#
+# Debian specific notes about encoder/decoder support.
+# #############################################################################
+# - Default MP3 encoder is set to "twolame" because this is the only supported
+#   package available in main section;
+# - You won't be able to encode to M4A/MP4 or decode from AAC, because "faac"
+#   package is not available in main section and "ffmpeg" support is disabled;
+# - You won't be able to encode to TTA because "ttaenc" package is not
+#   available in Debian at all.
+# #############################################################################
 
 # encoders: faac, ffmpeg
 # decoders: faad, ffmpeg, mplayer
@@ -46,7 +57,7 @@
 # encoders: ttaenc
 # decoders: ttaenc, ffmpeg, mplayer
 #
-TTA = ttaenc,ttaenc
+TTA = ttaenc,ffmpeg
 
 # encoders: wavpack
 # decoders: wvunpack, ffmpeg, mplayer
